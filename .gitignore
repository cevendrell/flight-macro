.DS_Store
.vscode/
.idea/
__pycache__/
*.pyc
.venv/
venv/
node_modules/
*.log

# Local warehouse artifacts if OVRHEAD_WAREHOUSE is ever pointed inside the repo
scripts/.cache/
*.duckdb
*.duckdb.wal
*.parquet
!data/*.json
!data/adsb/**/*.parquet

# Local cache of upstream route tables (routes.py). Lives in the warehouse on
# the collector; only ends up here when the repo is used as the warehouse.
data/adsb/routes-cache/
